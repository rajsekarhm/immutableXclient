import React from "react";
const Form = ({}) => {
  return (
    <div className="form">
      <form>
        <input onChange={() => {}} type="text" name="" />
        <input onChange={() => {}} type="text" name="" />
        <input onChange={() => {}} type="text" name="" />
        <input onChange={() => {}} type="text" name="" />
        <input onChange={() => {}} type="file" accept="image/*" />
        <button title="submit" onClick={() => {}} />
      </form>
    </div>
  );
};

export default Form;
